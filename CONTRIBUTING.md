# Contributing

Preserve the core rule: deterministic checks are stronger than model self-reports.

Before a pull request:

```bash
bash scripts/bash/validate-package.sh
```

Do not add new workflows unless they remove more complexity than they add. Do not make `/phasebatch` default. Do not make Codebase Memory default for every project.
