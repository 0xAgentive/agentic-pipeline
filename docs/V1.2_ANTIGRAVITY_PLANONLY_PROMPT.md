/planonly

Project: agentic-pipeline

Goal:
Design implementation phases for Agentic Pipeline v1.2.0 — Product Evidence Control Plane.

Do not implement code.
Do not edit templates.
Do not publish.
Do not push.
Do not run shipcheck.
Do not introduce mandatory cloud/API runtime.
Do not add write-capable MCP or autonomous multi-agent implementation.

Prerequisite:
Verify current v1.1.1b-r4/r4b stabilization status:
- local validator green;
- GitHub Actions green;
- fastpatch synthetic tests green;
- post-diff fastpatch required;
- untracked files included in fastpatch gate;
- no stale PHASE_STATUS;
- no failed current-main validation;
- patcher does not push by default.

If prerequisite is not green, stop and output only:
- blockers;
- exact corrective command;
- why v1.2 is blocked.

Use as source of truth:
- current repository;
- current docs/AGENTIC_PIPELINE_PLAYBOOK.md;
- .agents and templates;
- .agy state files;
- current CI;
- H10/Polar product lessons: LLM Pack, LLM Diary, multi-user, source configuration, reports, Trends, structured logs, visual quality, artifact loss, stale SHIP claims.

Required v1.2 design areas:

1. Compiled runtime:
   - runtime-src;
   - generated AGENTS.md, workflows, rules, skills, hooks;
   - full playbook reference-only.

2. Product contract layer:
   - PRODUCT_CONTRACT.json;
   - REQUIREMENTS_DELTA.ndjson;
   - stale shipcheck blocked by unresolved requirement changes.

3. Artifact Delivery Contract:
   - ARTIFACT_MANIFEST.json per material phase;
   - ARTIFACT_INDEX.ndjson;
   - evidence ZIP per audit/shipcheck/artifact-producing phase;
   - final response prints absolute path, relative path, size, SHA-256 and contents.

4. Evidence and metrics:
   - evidence.ndjson;
   - RUN_METRICS.ndjson;
   - markdown rendered from machine evidence.

5. Hard validators:
   - reference integrity;
   - runtime compiler check;
   - workflow/state/evidence/metrics schemas;
   - hook/skill/tool-profile validation;
   - product-contract validation;
   - report/artifact consistency validation.

6. Eval suite:
   - planonly_no_write;
   - auditphase_no_feature_write;
   - nextphase_one_phase_only;
   - fastpatch_blocks_backend;
   - fastpatch_sees_untracked;
   - fastpatch_requires_postdiff;
   - shipcheck_requires_evidence;
   - visualqa_required_for_ui;
   - reportqa_required_for_exports;
   - securityaudit_required_for_sensitive_data;
   - artifact_missing_blocks_audit;
   - artifact_contradiction_blocks_shipcheck;
   - requirements_delta_blocks_shipcheck.

7. Skills:
   - artifact-audit;
   - requirement-drift-audit;
   - release-evidence-packaging;
   - visual-qa;
   - report-qa;
   - security-privacy-review;
   - project-specific skills must stay narrow and optional.

8. Hook modes:
   - manual;
   - advisory;
   - enforcing;
   - hooks.json truthfulness;
   - hook contract smoke tests.

9. Workflow redesign:
   - /triage read-only JSON classifier;
   - /nextphase one phase only;
   - /fastpatch preflight + postdiff;
   - /visualqa deterministic artifacts;
   - /reportqa generated artifacts;
   - /securityaudit no writes by default;
   - /parallel-audit read-only lanes only;
   - /shipcheck refuses ready if tests/evidence/visual/report/security/rollback missing.

10. Tool/MCP profiles:
   - read-only default;
   - no write-capable MCP without approval;
   - no MCP as release truth;
   - Codebase Memory optional/read-only/raw-data-safe;
   - Sourcegraph deferred unless multi-repo/large/team.

11. Structured diagnostics reusable requirement:
   - for local apps with data, define app logs, redaction, diagnostics screen, diagnostic bundle;
   - no raw biometrics/secrets in diagnostic bundle.

12. Package builder, migration and rollback:
   - no push by default;
   - release archive verifies after extraction;
   - migration dry-run from v1.1.1b-r4/r4b;
   - rollback command exists.

Required output:
- current blockers;
- accepted lessons from Polar/H10;
- rejected or deferrable ideas;
- v1.2 file tree;
- phase list P0..Pn;
- for each phase:
  - goal;
  - risk;
  - files/components;
  - checks;
  - acceptance criteria;
  - stop conditions;
  - rollback/checkpoint notes;
- Definition of Done for v1.2;
- exact first /nextphase prompt for P0 only.

Stop after the plan.
