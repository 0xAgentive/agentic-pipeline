# Owner Output Presentation

Intermediate response:

1. work item;
2. material finding delta;
3. current route;
4. owner action only for a hard stop.

Final response:

1. product outcome;
2. remaining product/verification/release blockers;
3. checks;
4. up to three material artifact paths.

Do not show by default:

- SHA-256;
- byte sizes;
- `Exists: true`;
- project-relative paths;
- repeated `LOCAL ARTIFACTS` blocks;
- commands to open Explorer;
- a full replacement task brief for the same work item.
