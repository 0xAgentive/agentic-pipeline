# Runtime source scaffold

`runtime-src/` is a design scaffold for a future deterministic compiler. It is not currently the active source of generated `.agents` runtime files.

Until a compiler, source hashes and a drift validator exist, the active runtime remains the checked template under `templates/agy-project-base/.agents/`.
