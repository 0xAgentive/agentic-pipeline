# v1.1.1 Compatibility Guard

This file preserves historical compatibility constraints that remain valid under v1.2:

- model prose is not verification;
- fastpatch is script-gated;
- Codebase Memory on Windows must not rely on unsafe repository mirrors or junctions;
- deterministic semantic tests are required for critical logic.

The active framework contract is v1.2.0. When this compatibility note conflicts with v1.2 runtime/state/evidence rules, v1.2 wins.
