# Audit Coverage Matrix

Use when a GUARDED work item enters its first audit.

Map every acceptance outcome to actual product surfaces, evidence and deterministic checks. Publish the complete matrix before findings. Use stable coverage IDs. A later material finding outside the completed matrix is an audit-coverage miss.
