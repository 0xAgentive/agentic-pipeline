# Scientific Stage Firewall

During Protocol Freeze, distinguish protocol work from production analytical changes. An analytical defect requires an explicit algorithm-repair finding and bounded sub-scope. Preserve a new analytical baseline and return to freeze before validation.
