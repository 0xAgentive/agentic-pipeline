---
description: Codebase structural map.
---

# /codebase-map

Use Codebase Memory if visible. On Windows do not use CLI `index_repository`; use `node .\scripts\cbm-index-current-rpc.cjs` if reindexing is needed. Do not create mirrors, junctions, mklink, robocopy duplicates, or subst drives. Next command normally: `/auditphase`.
