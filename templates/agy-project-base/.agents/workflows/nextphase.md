# /nextphase

Execute the one owner-approved work item.

Before the first product write:
- import/confirm the action packet;
- activate the work item transaction;
- perform read-only discovery;
- bind exact paths and allowed commands atomically;
- validate the lease and stage firewall.

Build the initial candidate and deterministic receipts. Then publish a complete first audit or route to `/auditphase`. Do not stop for routine owner approval. Do not create a second semantic brief.
