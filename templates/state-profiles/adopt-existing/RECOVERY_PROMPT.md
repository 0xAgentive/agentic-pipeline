# Recovery Prompt

This workspace uses the `adopt-existing` state profile.

Run `/landing` first, then `/auditphase`. Do not migrate or implement feature work until the existing repository state is understood.
